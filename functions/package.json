{
  "name": "interactive-food-menu-functions",
  "private": true,
  "main": "lib/index.js",
  "engines": { "node": "22" },
  "scripts": {
    "build": "tsc",
    "test": "npm run build && node --test lib/*.test.js",
    "test:rules:direct": "npm run build && node --test lib/firestore.rules.integration.js",
    "test:e2e:direct": "node --test lib/functions.emulator.integration.js",
    "seed": "npm run build && node lib/setupVenue.js"
  },
  "dependencies": {
    "firebase-admin": "^14.3.0",
    "firebase-functions": "^7.3.2"
  },
  "devDependencies": {
    "@types/node": "^22.15.0",
    "@firebase/rules-unit-testing": "^5.0.0",
    "firebase": "^12.18.0",
    "typescript": "^5.9.2"
  }
}
